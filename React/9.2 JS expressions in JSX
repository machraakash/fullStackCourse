import React from "react";
import ReactDOM from "react-dom";

const luckyNumber = 7;

ReactDOM.render(
  <div>
    <h1>Your luckyNumber is {luckyNumber}!</h1>
  </div>,
  document.getElementById("root")
);